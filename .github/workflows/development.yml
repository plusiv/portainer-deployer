name: Pull Request to Main Action
on:
  pull_request:
    branches:
      - "main"
    paths:
      - "**/*.py"
      - "requirements.txt"
      - "Dockerfile"
      - "**/*.conf*"
      - ".github/workflows/development.yml"
    types: [opened, synchronize, reopened, labeled]

jobs:
  test:
    name: Test python package
    runs-on: ${{ matrix.os }} 
    strategy:
          matrix:
            os: [ubuntu-latest, macos-latest]
            python-version: [ '3.5', '3.6', '3.7', '3.8', '3.9', '3.10' ]
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v3
        with:
          python-version: ${{ matrix.python-version }} 
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      - name: Create config files
        run: |
          touch portainer_deployer/.env
          echo "[CONFIG]" >> portainer_deployer/.env
          echo "PATH_TO_CONFIG=./app.conf" >> portainer_deployer/.env
          cp portainer_deployer/app.conf.example portainer_deployer/app.conf
      - name: Run tests
        run: python -m unittest tests/*
      - name: Test installation
        run: | 
          python setup.py install
          portainer-deployer -v
  